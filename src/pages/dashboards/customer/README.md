# Customer Dashboard

Protected customer workspace pages live here. Keep customer-specific route composition and dashboard UI in this folder; keep reusable commerce behavior in `src/features` and shared controls in `src/components/ui`.
